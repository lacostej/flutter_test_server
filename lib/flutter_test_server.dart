/// A lightweight HTTP server for Flutter apps that exposes a REST API
/// for on-device test automation — inspired by Unium for Unity.
///
/// Drop `FlutterTestServer` into your app and drive tests with `curl`.
library flutter_test_server;

export 'src/server.dart' show FlutterTestServer;
